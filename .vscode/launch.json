{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debuger for Firefox",
            "type": "firefox",
            "request": "launch",
            "reAttach": true,
            "file": "${workspaceFolder}/index.html"
        }
    ]
}